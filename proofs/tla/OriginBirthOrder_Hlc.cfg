\* HLC: a new origin outranks every origin already born.

SPECIFICATION Spec
CONSTANTS
    Ranks = {1, 2, 3}
    NoOwner = none
    Replicas = {a, b}
    MaxSeq = 1
    MonotoneBirth = TRUE
INVARIANT
    TypeOK
    NoLiveOriginBelowWatermark
    ReapedMeansLevel
