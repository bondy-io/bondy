\* Does draining the WHOLE inbox save the shipped rule? No: arrival timing splits batches whatever the drain does. EXPECT: NoOverClaim VIOLATED.
SPECIFICATION Spec

CONSTANTS
    Origins = {o1}
    Buckets = {b1, b2}
    MaxSeq = 3
    Unroutable = b2
    ClaimRule = "MaxCap"
    InOrderDelivery = FALSE
    WholeBatchOnly = TRUE
    CanHeal = TRUE
    CanRefold = TRUE
    CanRestart = FALSE
    DoorTruncates = TRUE
    TruncatePrefixOnly = TRUE
    DoorUsesPending = FALSE

INVARIANTS TypeOK NoOverClaim
