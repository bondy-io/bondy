\* The fix with the door sharpened to the exact test. Also clean -- and it buys nothing, because the truncation cap already holds everything above the hole. See the _Uncapped run for what it would cost if the cap ever went away.
SPECIFICATION Spec

CONSTANTS
    Origins = {o1}
    Buckets = {b1, b2}
    MaxSeq = 3
    Unroutable = b2
    ClaimRule = "Pending"
    InOrderDelivery = FALSE
    WholeBatchOnly = FALSE
    CanHeal = TRUE
    CanRefold = FALSE
    CanRestart = FALSE
    DoorTruncates = TRUE
    TruncatePrefixOnly = TRUE
    DoorUsesPending = TRUE

INVARIANTS TypeOK NoOverClaim PendingSound NoLoss Exact Complete
