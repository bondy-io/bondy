\* Wall-clock-prefixed ids (UUIDv7 / ULID / KSUID): a lagging or rolled-back
\* clock may mint an origin that sorts BELOW ids already born.
SPECIFICATION Spec
CONSTANTS
    Ranks = {1, 2, 3}
    NoOwner = none
    Replicas = {a, b}
    MaxSeq = 1
    MonotoneBirth = FALSE
INVARIANT
    TypeOK
    NoLiveOriginBelowWatermark
    ReapedMeansLevel
