\* The same rule asked whether it ever recovers, with nothing to re-present the oplog: it does not. EXPECT: EventuallyComplete VIOLATED -- sound but dead.
SPECIFICATION FairSpec

CONSTANTS
    Origins = {o1}
    Buckets = {b1, b2}
    MaxSeq = 3
    Unroutable = b2
    ClaimRule = "Contig"
    InOrderDelivery = FALSE
    WholeBatchOnly = FALSE
    CanHeal = TRUE
    CanRefold = FALSE
    CanRestart = FALSE
    DoorTruncates = TRUE
    TruncatePrefixOnly = TRUE
    DoorUsesPending = FALSE

PROPERTY EventuallyComplete
