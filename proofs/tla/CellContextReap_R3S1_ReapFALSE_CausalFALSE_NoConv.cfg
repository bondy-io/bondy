CONSTANTS
    Replicas = {r1, r2, r3}
    MaxSeq = 1
    ReapEnabled = FALSE
    CausalDelivery = FALSE

SYMMETRY Symm

\* Convergence omitted so the resurrection question is answered
\* exhaustively even in the regime where Convergence fails early.
INVARIANTS
    TypeOK
    NoResurrection
    NoPhantomDot
    ContextCoversDots

SPECIFICATION Spec
CHECK_DEADLOCK FALSE
