\* The consequence, against the door AS IMPLEMENTED (prefix-capped). The over-claim disarms the very hold that would have kept the event: `never_applied/2` judges against the frontier. EXPECT: NoLoss VIOLATED -- an event truncated from the tree that the projection never folded.
SPECIFICATION Spec

CONSTANTS
    Origins = {o1}
    Buckets = {b1, b2}
    MaxSeq = 3
    Unroutable = b2
    ClaimRule = "MaxCap"
    InOrderDelivery = FALSE
    WholeBatchOnly = FALSE
    CanHeal = TRUE
    CanRefold = TRUE
    CanRestart = FALSE
    DoorTruncates = TRUE
    TruncatePrefixOnly = TRUE
    DoorUsesPending = FALSE

INVARIANTS TypeOK NoLoss
