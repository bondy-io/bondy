\* As Ckpt, but minting may run before the WAL tail has replayed -- what the code allows.
SPECIFICATION Spec
CONSTANTS
    MaxSeq = 3
    SeedRule = "ckpt"
    FrontierBeforeTruncate = FALSE
    RetentionKeyedOnCkpt = TRUE
    MintAfterReplay = FALSE
INVARIANTS TypeOK DurableSound SeedExact DotUnique
