\* NEGATIVE CONTROL: an unroutable bucket is NOT a contiguity hole in the fold.
SPECIFICATION Spec

CONSTANTS
    Replicas = {r1, r2}
    Buckets = {b1, b2}
    MaxSeq = 2
    SkipIsHole = TRUE
    ContigClaim = FALSE
    GateOnRegistration = TRUE
    ServeGate = TRUE
    AdoptIfComplete = TRUE
    GapVerdict = TRUE
    VersionSkew = TRUE
    SkewReplica = r2
    SkewBucket = b2

INVARIANT
    TypeOK
    NoOverClaim
    NoStrandedCell

CHECK_DEADLOCK FALSE
