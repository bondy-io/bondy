\* ... and not complete ON THE SPOT: a seq folded while a hole was open is forgotten by the integer, so after everything arrives, every bucket routes and the projection holds it all, the entry still under-reports. EXPECT: Complete VIOLATED. Compare FrontierPending_Pending.cfg -- same invariant, clean.
SPECIFICATION Spec

CONSTANTS
    Origins = {o1}
    Buckets = {b1, b2}
    MaxSeq = 3
    Unroutable = b2
    ClaimRule = "Contig"
    InOrderDelivery = FALSE
    WholeBatchOnly = FALSE
    CanHeal = TRUE
    CanRefold = FALSE
    CanRestart = FALSE
    DoorTruncates = TRUE
    TruncatePrefixOnly = TRUE
    DoorUsesPending = FALSE

INVARIANTS TypeOK Complete
