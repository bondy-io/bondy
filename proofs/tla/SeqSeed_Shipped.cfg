\* What ships (granting it a replay-before-mint it does not enforce): SeqRef seeded from the live tree, the WAL tail tops it up as it replays.
SPECIFICATION Spec
CONSTANTS
    MaxSeq = 3
    SeedRule = "live"
    FrontierBeforeTruncate = FALSE
    RetentionKeyedOnCkpt = TRUE
    MintAfterReplay = TRUE
INVARIANTS TypeOK DurableSound SeedExact DotUnique
