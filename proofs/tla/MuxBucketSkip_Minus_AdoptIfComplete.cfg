\* NEGATIVE CONTROL: bootstrap adopts the peer frontier unconditionally (the SHIPPED rule).
SPECIFICATION Spec

CONSTANTS
    Replicas = {r1, r2}
    Buckets = {b1, b2}
    MaxSeq = 2
    SkipIsHole = TRUE
    ContigClaim = TRUE
    GateOnRegistration = TRUE
    ServeGate = TRUE
    AdoptIfComplete = FALSE
    GapVerdict = TRUE
    VersionSkew = TRUE
    SkewReplica = r2
    SkewBucket = b2

INVARIANT
    TypeOK
    NoOverClaim
    NoStrandedCell

CHECK_DEADLOCK FALSE
