\* SHIPPED RULE, `bondy_oplog_cell_apply:claim/2`. Batch boundaries are not chosen, so a seq that failed -- or never arrived -- before this batch is invisible to it. EXPECT: NoOverClaim VIOLATED.
SPECIFICATION Spec

CONSTANTS
    Origins = {o1}
    Buckets = {b1, b2}
    MaxSeq = 3
    Unroutable = b2
    ClaimRule = "MaxCap"
    InOrderDelivery = FALSE
    WholeBatchOnly = FALSE
    CanHeal = TRUE
    CanRefold = TRUE
    CanRestart = FALSE
    DoorTruncates = TRUE
    TruncatePrefixOnly = TRUE
    DoorUsesPending = FALSE

INVARIANTS TypeOK NoOverClaim
