\* Baseline: the system as implemented -- gap check ON, compaction
\* NOT gated (mst_retention / recency-filtered frontier).
SPECIFICATION Spec

CONSTANTS
    Replicas = {r1, r2, r3}
    MaxSeq = 2
    GapCheckEnabled = TRUE
    PrefixHold = FALSE
    GatedCompaction = FALSE

INVARIANT
    TypeOK
    NoOverClaim
    PrefixClosed
