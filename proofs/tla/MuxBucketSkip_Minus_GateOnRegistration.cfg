\* NEGATIVE CONTROL: initiator syncs and folds before its own directory is complete.
SPECIFICATION Spec

CONSTANTS
    Replicas = {r1, r2}
    Buckets = {b1, b2}
    MaxSeq = 2
    SkipIsHole = TRUE
    ContigClaim = TRUE
    GateOnRegistration = FALSE
    ServeGate = TRUE
    AdoptIfComplete = TRUE
    GapVerdict = TRUE
    VersionSkew = TRUE
    SkewReplica = r2
    SkewBucket = b2

INVARIANT
    TypeOK
    NoOverClaim
    NoStrandedCell

CHECK_DEADLOCK FALSE
