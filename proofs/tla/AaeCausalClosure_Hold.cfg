\* THE FIX (increment 2, db.aae.prefix_hold = on, shipped default): hold
\* non-contiguous suffixes at the fold. Exhaustive at the PAIRWISE
\* protocol scope (2 replicas): sync, hold and frontier logic are all
\* pairwise; the 3-replica space exploded past practical bounds once
\* Rebootstrap faithfully retains own-origin events, gap check on, compaction UNGATED
\* (the recency-filtered reality). Expect exhaustive clean.
SPECIFICATION Spec

CONSTANTS
    Replicas = {r1, r2}
    MaxSeq = 2
    GapCheckEnabled = TRUE
    PrefixHold = TRUE
    GatedCompaction = FALSE

INVARIANT
    TypeOK
    NoOverClaim
    PrefixClosed
