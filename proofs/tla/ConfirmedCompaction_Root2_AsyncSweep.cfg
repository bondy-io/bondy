\* The shipped rule, applier-backed, ETS page GC. Safety is expected to
\* hold (the root rule is sound, only stalled); RoundConfirmsApplied is
\* expected violated.
SPECIFICATION Spec
CONSTANTS
    Replicas = {1, 2}
    MaxSeq = 2
    Skew = 1
    Rule = "root"
    AsyncApply = TRUE
    Sweep = "all"
    CapAtUnapplied = FALSE
INVARIANTS TypeOK NoLoss PrefixClosed
PROPERTIES NoDrop RoundConfirmsApplied
