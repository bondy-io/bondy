# FLY_FAULT_CAPABILITY.md

**M0 fault-injection capability matrix** — generated by `run-spike.sh`.

- Bondy build: `b9576e33` (tag `none`)
- App: `bondy-perf-m0` · org `leapsight` · region `lhr` · nodes: `3`
- Generated: `2026-07-27T17:01:40Z`

Legend: ✅ confirmed on every node · ⚠️ partial · ❌ not confirmed / unavailable.
A fault is only trusted downstream (03 §5, 05 R1) when its row is ✅.

| Fault (probe id) | 03 §5 class | Nodes attempted | Nodes confirmed | Status |
|---|---|---|---|---|
| `net_admin_cap` | precondition (CAP_NET_ADMIN) | 3/3 | 3/3 | ✅ |
| `partition_iptables` | partition | 3/3 | 3/3 | ✅ |
| `delay_tc_netem` | delay / loss | 3/3 | 3/3 | ✅ |
| `clock_skew_libfaketime` | clock_skew | 3/3 | 3/3 | ✅ |
| `disk_fill_fallocate` | disk fill | 3/3 | 3/3 | ✅ |
| `cpu_stress_stressng` | cpu stress | 3/3 | 3/3 | ✅ |
| `signal_beam` | kill / pause (signal) | 3/3 | 3/3 | ✅ |

## Fallback routing (03 §5)

Any row that is not ✅ routes its fault class to the local-Docker substrate
(`just m0-local-*` / 03 §6) for the runs that need it. No downstream milestone
trusts a Fly network/clock fault whose row here is not ✅.

## Raw probe output

Per-node NDJSON is in `harness/m0-fly-spike/out/<machine-id>.ndjson`.
