{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
    {apps, [bondy_cbor]}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info, nowarn_export_all]},
        {deps, []}
    ]}
]}.

{eunit_opts, [verbose]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.
